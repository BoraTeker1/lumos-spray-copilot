# Lower bounds chosen for broad Python-version compatibility (incl. 3.13).
# Pip will resolve to the newest compatible wheels.
fastapi>=0.111
uvicorn[standard]>=0.30
sqlalchemy>=2.0.30
pydantic>=2.8
httpx>=0.27
python-multipart>=0.0.9   # file uploads (field-photo analysis)
anthropic>=0.40           # multimodal photo analysis (optional; falls back to a mock without a key)
alembic>=1.13             # schema migrations (baseline exists; required once real pilot data lands)
pytest>=8.2
openpyxl>=3.1            # reads the USDA PDP reference workbook (.xlsx, 2020+ releases)
xlrd>=2.0                # same workbook in the legacy .xls form (2019 and earlier releases)
